replicaCount: 200

service:
  type: ClusterIP

resources:
  requests:
    cpu: 20m
    memory: 32Mi
  limits:
    cpu: 50m
    memory: 64Mi

# autoscaling:
#   enabled: true
#   minReplicas: 10
#   maxReplicas: 10
#   targetCPU: 20
